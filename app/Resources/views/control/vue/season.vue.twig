<div class="panel" v-bind:class="[season.id == currentSeasonId ? 'panel-primary' : 'panel-info']">
    <div class="panel-heading">
        <div class="row">
            <div class="col-xs-6">
                <h3 class="panel-title season-title">{{ season.year - 1 }}/{{ season.year }}</h3>
            </div>
            <div class="col-xs-6 text-right">
                <button v-if="season.id != currentSeasonId" v-on:click="markCurrent" class="btn btn-xs btn-default" title="Сделать текущим сезоном"><i class="fa fa-check" aria-hidden="true"></i></button>
                <button class="btn btn-xs btn-default" title="Копировать"><i class="fa fa-files-o" aria-hidden="true"></i></button>
                <button class="btn btn-xs btn-danger" title="Удалить"><i class="fa fa-trash-o" aria-hidden="true"></i></button>
            </div>
        </div>
    </div>
    <table class="table">
        <thead>
        <tr>
            <th>Команда</th>
            <th>Тренер</th>
            <th>Лига</th>
            <th></th>
        </tr>
        </thead>
        <tbody>
        <tr v-for="seasonteam in season.seasonteams">
            <td>{{ seasonteam.team.metadata.title }}</td>
            <td><a>Имя</a></td>
            <td><a></a></td>
            <td class="text-right">
                <button class="btn btn-xs btn-info" title="Редактировать"><i class="fa fa-edit" aria-hidden="true"></i></button>
                <button class="btn btn-xs btn-danger" title="Удалить"><i class="fa fa-trash-o" aria-hidden="true"></i></button>
            </td>
        </tr>
        <tr>
            <td colspan="4"><button class="btn btn-xs btn-primary" v-on:click="onSeasonTeamAddClick">Добавить команду в сезон</button></td>
        </tr>
        </tbody>
    </table>
</div>